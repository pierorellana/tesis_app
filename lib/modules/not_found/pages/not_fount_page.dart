import 'package:flutter/material.dart';
import '../widgets/drawer_404.dart';

class PageNotFound extends StatelessWidget {
  const PageNotFound({super.key});

  @override
  Widget build(BuildContext context) {
    return drawerWidget404(context);
  }
}
